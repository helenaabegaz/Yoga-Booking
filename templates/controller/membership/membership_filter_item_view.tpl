<div class="filter-item tab">
    <div class="inner">
        <div class="ft-input">
            <div class="custom-checkbox">
                <label for="service-id{{id}}">
                    <input id="service-id{{id}}" type="checkbox" {{#if checked}}checked="checked"{{/if}} />
                    <span>{{name}}</span>
                </label>
            </div>
        </div>
        <!--
        <div class="ft-label">
            <label for="service-id{{id}}">
                {{name}}
            </label>
        </div>
        -->
    </div>
</div>